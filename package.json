{
  "name": "teamtask",
  "private": true,
  "version": "1.0.0",
  "description": "Free desktop/mobile team task app with checklists and notifications",
  "scripts": {
    "seed": "npm --prefix server run seed",
    "server": "npm --prefix server start",
    "server:dev": "npm --prefix server run dev",
    "app": "npm --prefix app start",
    "web": "npm --prefix app run web",
    "android": "npm --prefix app run android",
    "setup": "npm --prefix server install && npm --prefix app install && npm --prefix server run seed",
    "export:web": "npm --prefix app exec -- expo export --platform web",
    "build:windows": "build-windows.cmd",
    "desktop": "npm --prefix desktop start"
  }
}
